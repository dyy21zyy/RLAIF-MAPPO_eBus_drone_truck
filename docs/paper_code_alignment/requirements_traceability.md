# Paper-Code Alignment Requirements Traceability

This document maps the current paper contract to code, tests, and validation
evidence. The confirmed final manuscript file has not yet been supplied in this
checkout, so unresolved manuscript-dependent claims remain marked as
`blocked-by-user-decision`.

| ID | Paper claim | Required behavior | Code area | Test | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| REQ-DATA-001 | The formal setting uses real transit data where available and preserves source boundaries. | Real observations, inherited eBus-Drone defaults, explicit truck-extension defaults, and fallback smoke fixtures must remain distinguishable. | `data_pipeline/`, `docs/DATA_SETTING_TRACE.md`, `docs/RANDOMNESS_DESIGN.md` | `tests/test_original_scale_real_transit.py`, `tests/test_documentation_consistency.py` | satisfied | Existing trace docs and tests preserve source labels and prohibit fake real data claims. |
| REQ-MDP-001 | Parcel delivery is modeled as an event-driven MDP with TD, TBD, and TLD actions. | A `step` resolves one real decision event; assignment actions cover TD, TBD via station, and TLD via station; bus arrival actions cover charging durations. | `envs/delivery_env.py`, `docs/MDP_SPECIFICATION.md` | `tests/test_stage3_environment.py`, `tests/test_candidate_action_schema.py` | satisfied | Stage 3 tests run complete episodes and check masks/invariants. |
| REQ-RLAIF-001 | AI or human preference labels are the only source of RLAIF supervision. | Offline mode must not create labels; replay/API labels must be validated; objective features and rules must never become fabricated labels or rewards. | `rlaif/`, `experiments/build_ai_preferences.py`, `training/reward_model_wrapper.py` | `tests/test_rlaif_data.py`, `tests/test_preference_dataset.py`, `tests/test_reward_model_wrapper.py` | satisfied | Preference workflow docs and tests reject fabricated/rule labels. |
| REQ-MAPPO-001 | MAPPO is asynchronous over real decision events. | Assignment actor is active only at parcel-release decisions; inactive-agent rows are not fabricated. | `training/mappo_async.py`, `training/mappo_buffer.py`, `training/mappo_trainer.py` | `tests/test_mappo_async.py`, `tests/test_mappo_buffer.py` | superseded-by-stage-9 | Stage 9 expands this boundary to assignment, truck, bus, and station agents. |
| REQ-EXP-001 | Smoke tests are code gates, not final paper experiments. | Missing PyTorch or missing learned checkpoints must skip clearly; no smoke artifact can be reported as final benchmark evidence. | `experiments/`, `docs/EXPERIMENTS.md`, `README.md` | `tests/test_experiment_configs.py`, `tests/test_evaluation_runner.py`, `tests/test_stage5_code_gate.py` | satisfied | Current gates distinguish code validation from deferred runtime experiments. |
| REQ-RUNTIME-001 | Final RLAIF-enabled PPO/MAPPO experiments require a valid PyTorch runtime and trained checkpoints. | Stage 5 Runtime Gate, reward model checkpoint, Stage 6/7/9 checkpoints, and configured benchmark runs are required before result claims. | `experiments/train_reward_model.py`, `experiments/train_assignment_ppo.py`, `experiments/train_mappo_async.py` | `tests/test_stage5_code_gate.py`, `tests/test_assignment_ppo.py`, `tests/test_mappo_async.py` | partially-satisfied | Code treats broken PyTorch as runtime unavailable; actual training remains deferred. |
| REQ-PAPER-001 | The final manuscript is the authority when paper and code conflict. | Every final manuscript claim must be extracted and mapped to code/tests before implementation. | `docs/paper_code_alignment/` | `tests/test_paper_code_alignment_docs.py` | blocked-by-user-decision | Final `.tex` source was not provided in this checkout. |
| REQ-SEM-001 | Delivery-mode semantics must match the final manuscript. | If the final paper changes TD/TBD/TLD or TO/BLD/TLD wording, environment, reward accounting, and docs must be updated and tested together. | `envs/delivery_env.py`, `envs/state_builder.py`, `docs/MDP_SPECIFICATION.md` | Future focused MDP tests | blocked-by-user-decision | Current repo docs and implementation preserve the latest coded action semantics. |
| REQ-RLAIF-002 | RLAIF scope must match the final manuscript. | If the final paper says RLAIF shapes truck, bus, or station decisions, Stage 9 reward integration must be expanded after real labels exist. | `training/mappo_async.py`, `training/mappo_trainer.py`, `training/reward_model_wrapper.py` | Future reward-attribution tests | blocked-by-user-decision | Learned RLAIF remains assignment-only until real multi-agent labels/checkpoints exist. |
| REQ-MDP-FOUR-AGENT | The method trains assignment, truck, bus, and station agents over the asynchronous event stream. | Expose real decision events for assignment, truck, bus, and station without inactive-agent padding. | `envs/delivery_env.py`, `envs/state_builder.py` | `tests/test_four_agent_environment.py` | implemented | Four operational event types are observed in a real episode without dummy inactive agents. |
| REQ-MDP-CANDIDATES | Candidate-action encoding and masks represent operational constraints. | Every decision observation includes candidate descriptors, finite candidate features, and hard masks. | `envs/decision_schema.py`, `envs/state_builder.py` | `tests/test_four_agent_candidate_schema.py` | implemented | `DecisionSurface` exports masks, payloads, and finite candidate matrices. |
| REQ-MDP-TRANSITIONS | Truck dispatch, bus loading and charging, and station drone/battery operations are real operational decisions. | Environment action appliers update parcel, truck, bus, station, locker, drone, battery, and reward state. | `envs/delivery_env.py` | `tests/test_four_agent_environment.py` | implemented | Four operational event types are exposed as policy decision surfaces. |
| REQ-MAPPO-ACTORS | Heterogeneous event-specific actors train under a centralized critic. | Provide actor registry entries for assignment, truck, bus, and station. | `training/mappo_networks.py`, `training/mappo_trainer.py` | `tests/test_mappo_networks.py`, `tests/test_mappo_async.py` | implemented | `build_actor_registry()` and Stage 9 smoke cover all four actor ids. |
| REQ-MAPPO-CANDIDATE-POLICY | The actor scores candidate actions and applies masks before softmax. | Candidate-scoring actors give infeasible actions zero probability. | `training/mappo_networks.py` | `tests/test_mappo_networks.py` | implemented | `CandidateScoringActor` mask test covers variable candidate sets. |
| REQ-MAPPO-BUFFER | Rollouts store only activated agents and use event-time discounting. | Buffer supports four agent ids, no inactive rows, and time-delta GAE. | `training/mappo_buffer.py` | `tests/test_mappo_buffer.py` | implemented | Four-agent buffer and elapsed-minute discount tests cover this contract. |
| REQ-RLAIF-SCOPE | AI feedback ranks feasible alternatives, while physical feasibility remains masked. | Disabled RLAIF loads no checkpoint; enabled RLAIF requires a valid checkpoint; no rules create labels or rewards. | `rlaif/`, `training/reward_model_wrapper.py`, `training/mappo_async.py` | `tests/test_reward_model_wrapper.py`, `tests/test_mappo_async.py` | implemented-with-assignment-scope | RLAIF reward remains assignment-only until real multi-agent labels/checkpoints exist. |
| REQ-DOC-TRACE | Paper-code alignment decisions and validation remain auditable. | Maintain traceability, decision, and validation files for the four-agent pass. | `docs/paper_code_alignment/` | `tests/test_paper_alignment_traceability.py`, `tests/test_documentation_consistency.py` | implemented | Documentation tests require Stage 9 four-agent wording and no-fabrication boundaries. |

Smoke-test metrics are code-gate evidence only and are not final experiment evidence.
