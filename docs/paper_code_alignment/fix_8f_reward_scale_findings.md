# Step 6A reward scale findings

| file | function or class | current behavior | required behavior | failing test |
|---|---|---|---|---|
| `experiments/estimate_reward_reference_scales.py` | legacy estimator | Estimated from a single config/env and inferred raw values from weighted costs; wrote directly to outputs. | Require frozen train bank, real reset/step rollouts per policy/scenario, raw component rows, stats, artifact hash, and no formal output generation in this PR. | `tests/test_reward_scale_uses_train_bank_only.py` |
| `envs/reward_scales.py` | `load_reward_scale_artifact` | Did not validate training-bank lineage, run classification, component status, validation status, or formal diagnostic rejection. | Validate artifact type/version/hash, bank hash, component order/status/scales, and reject diagnostic/blocked artifacts in formal mode. | `tests/test_reward_scale_artifact_classification.py` |
| `envs/reward_ledger.py` | `RewardLedger.add_cost` | Ledger entries did not persist reference scale, configured weight, reward contribution, or scale artifact hash. | Preserve raw, scale, normalized, weight, weighted, contribution, and scale lineage per component. | `tests/test_reward_scale_reward_ledger_reconciliation.py` |
| `envs/delivery_env.py` | reward setup and `_charge_cost` | Used a local component tuple and ledger was called without the active reference scale. | Use canonical component registry and pass scale lineage into the ledger while keeping raw values unchanged. | `tests/test_reward_scale_environment_application.py` |
| `training/config_resolver.py` | formal config validation | Placeholder bank/scale hashes were not fully tied to artifact validation. | Formal configs must retain explicit blockers until real formal bank and scale hashes are inserted. | `tests/test_reward_scale_readiness_gate.py` |
| `training/mappo_trainer.py` | checkpoint metadata | Stored only a limited scale hash field. | Preserve artifact path, hash, training-bank hash, artifact version, and estimator in checkpoints. | `tests/test_reward_scale_policy_checkpoint_lineage.py` |
| `evaluation/result_schema.py` and benchmark runners | benchmark rows | Scale lineage could be absent from benchmark rows/manifests. | Record reward-scale artifact path, artifact hash, and training-bank hash in benchmark output lineage. | `tests/test_reward_scale_benchmark_lineage.py` |
| `.gitignore` | generated artifacts | Reward-scale diagnostic/final outputs were not explicitly ignored. | Ignore diagnostic reward-scale trees and generated `outputs/reward_reference_scales*.json`. | `tests/test_reward_scale_no_repo_generated_artifacts.py` |
