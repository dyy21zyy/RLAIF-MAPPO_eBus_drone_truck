# Fix 8h Pre-formal Gate Findings

These are pre-formal validation runs. They are not final paper experiments.

| Gap | File | Function or missing component | Current behavior | Required behavior | Failing test |
| --- | --- | --- | --- | --- | --- |
| End-to-end pre-formal controller | `experiments/run_readiness_pilot.py` | Missing pre-formal controller | Runs a readiness pilot and reports environment/RLAIF readiness, but does not connect scenario banks, reward scales, reward-model validation, benchmark, ablation, sensitivity, paired validation, and launch planning. | One controller must orchestrate the complete pre-formal workflow. | `tests/test_preformal_gate.py::test_canonical_stage_manifest_and_status_contract` |
| Canonical stage manifest | `evaluation/experiment_jobs.py` | Missing `PREFORMAL_STAGES` | Jobs exist for experiment execution, but no canonical ordered pre-formal manifest records all required stages. | Define the ordered pre-formal stage list and per-stage records. | `tests/test_preformal_gate.py::test_canonical_stage_manifest_and_status_contract` |
| Dependency enforcement | `experiments/validate_formal_experiment_readiness.py` | Missing stage dependency propagation | Readiness validation reports blockers, but dependent workflow stages are not represented as blocked records. | Required downstream stages must become `blocked_dependency` when upstream stages fail. | `tests/test_preformal_gate.py::test_dependency_blocks_downstream_after_required_failure` |
| Formal-candidate artifact inventory | `evaluation/formal_policy_registry.py` | Partial checkpoint validation only | Policy checkpoint checks exist, but there is no workflow-wide inventory for scenario, reward-scale, reward-model, policy, and result artifacts. | Inventory formal candidates with paths, hashes, existence, and binary suffix policy. | `tests/test_preformal_gate.py::test_strict_rejects_diagnostic_artifact` |
| Complete pre-formal pass/fail criteria | `experiments/run_readiness_pilot.py` | `classify` | Existing statuses are readiness-pilot statuses, not the required pre-formal statuses. | Return exactly one required pre-formal status and never generic success. | `tests/test_preformal_gate.py::test_diagnostic_success_uses_diagnostic_status_not_strict_success` |
| Final experiment launch commands | `configs/paper/` | Missing generated pre-formal launch plan | Paper configs define runnable pieces, but no pre-formal gate emits exact final launch commands after validation. | Generate a formal launch plan after paired validation and sensitivity pass. | `tests/test_preformal_gate.py::test_canonical_stage_manifest_and_status_contract` |
